name: sazones_semanales
description: "Marketplace Startup offering On-Demand Private Chefs dedicated to weekly Meal Prep services."

version: 1.0.0+1

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  postgres: ^3.4.8
  cupertino_icons: ^1.0.2
  flutter_dotenv: ^5.1.0
  http: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true